config:
  target: 'http://localhost:3002'
  phases:
    - duration: 200
      arrivalRate: 500
  payload:
    path: "dataAbodeId.csv"
    fields:
      - "abodeid"
  defaults:
    headers:
      x-my-service-auth: '987401838271002188298567'
scenarios:
  - flow:
    - get:
        url: "/abodes/{{abodeid}}"
